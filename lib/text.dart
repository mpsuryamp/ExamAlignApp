import 'package:flutter/material.dart';

class texts extends StatelessWidget {
  const texts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('sdfghjkvbnm',style: TextStyle(fontSize: 20),)),
    );
  }
}